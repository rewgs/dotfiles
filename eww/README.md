Symlink [`dots`](./dots/) to `~/.config/eww`.

At the moment, the contents of [`dots`](./dots/) are just the bar example from the eww examples.

Config documentation here: https://elkowar.github.io/eww/configuration.html

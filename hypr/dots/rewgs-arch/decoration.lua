hl.config({
    decoration = {
        rounding         = 3,  -- 0 is square; > 0 makes corners more round
        active_opacity   = 1.0,
        inactive_opacity = 1.0,

        blur = {
            enabled = false,
        },

        shadow = {
            enabled = false,
        },
    },
})

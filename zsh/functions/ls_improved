# TODO: make the sorting case-insensitive

function ls_improved () {
    if [[ $(uname) == "Darwin" ]]; then
        if [[ $# -eq 0 ]]; then
            gls --color -alh --group-directories-first
        else
            gls "${1}" --color -alh --group-directories-first
        fi
    elif [[ $(uname) == "Linux" ]]; then
        if [[ $# -eq 0 ]]; then
            ls -alh --color
        else
            ls "${1}" -alh --color
        fi
    fi
}

# editors that aren't (n)vim
alias h="hx"    # helix
alias k="kak"   # kakoune
alias n="nano"  # nano

alias fetch="fastfetch"
# alias fetch="neofetch"

alias f="fzf"

# sed
if [[ $(uname) == "Darwin" ]]; then
    alias sed="gsed"
fi

# xplr
alias x="xplr"

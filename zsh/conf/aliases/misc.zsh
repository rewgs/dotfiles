# editors that aren't (n)vim
alias h="hx"    # helix
alias k="kak"   # kakoune
alias n="nano"  # nano

alias f="fzf"

# alias fetch="neofetch"
alias fetch="fastfetch"

# sed
if [[ $(uname) == "Darwin" ]]; then
    alias sed="gsed"
fi

# xplr
alias x="xplr"

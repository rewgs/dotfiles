activate_venv () {
    source .venv/bin/activate
    which_python=$(which python)
    python_version=$(python --version)
    echo -e "\nUsing:"
    echo -e "\t${python_version}"
    echo -e "located at:"
    echo -e "\t${which_python}\n"
}

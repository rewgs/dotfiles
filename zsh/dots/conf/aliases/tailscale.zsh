# tailscale

# NOTE: Removed because the golden path is to symlink this to /usr/local/bin/tailscale
# if [[ $(uname) == "Darwin" ]] && \
# [[ ! -f "$HOME/go/bin/tailscale" ]] && \
# [[ -f "/Applications/Tailscale.app/Contents/MacOS/Tailscale" ]]; then
#     alias tailscale="/Applications/Tailscale.app/Contents/MacOS/Tailscale"
# fi

# NOTE: Removed because I never use this.
# alias tshosts=tailscale_get_magic_dns_hostnames
